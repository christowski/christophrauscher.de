{% extends "layouts/base.njk" %} 
{% block content %}
  <div class="pageheader">
    <h1>{{ title }}</h1>
    <p>{{ description }}</p>
  </div>
  <section class="drawings-grid">
    {{ content | safe }}
  </section>
{% endblock %}